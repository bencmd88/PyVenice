[project]
name = "menace"
version = "0.1.0"
description = "A multi-prompt app for the venice.ai API."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
    "python-dateutil>=2.8.0",
    "typing-extensions>=4.0.0",
]
